export const metadata = {
  title: 'Générateur CSS',
  description: 'Générez du CSS visuellement : box-shadow, border-radius, gradient, text-shadow et animations — avec aperçu en temps réel.',
};
export default function Layout({ children }) { return children; }
