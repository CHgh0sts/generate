export const metadata = {
  title: 'Générateur d\'ASCII Art',
  description: 'Convertissez du texte en ASCII art avec différentes polices — export texte, copie instantanée, 100% local.',
  alternates: { canonical: '/ascii' },
};
export default function Layout({ children }) { return children; }
