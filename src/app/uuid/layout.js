export const metadata = {
  title: 'Générateur UUID / Timestamp',
  description: 'Générez des UUID v4, UUID v1, NanoID et convertissez des timestamps UNIX en dates humaines — gratuit, instantané.',
  alternates: { canonical: '/uuid' },
};
export default function Layout({ children }) { return children; }
