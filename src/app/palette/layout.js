export const metadata = {
  title: 'Palette de couleurs depuis une image',
  description: 'Extrayez les couleurs dominantes d\'une photo — HEX, RGB, HSL — 100% navigateur via k-means clustering.',
};
export default function Layout({ children }) { return children; }
