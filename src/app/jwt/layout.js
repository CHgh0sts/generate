export const metadata = {
  title: 'Décodeur JWT',
  description: 'Décodez, inspectez et analysez vos JSON Web Tokens (JWT) — header, payload, expiration — directement dans le navigateur.',
};
export default function Layout({ children }) { return children; }
